{
  "project": "backendMain",
  "description": "Project-level configuration manifest. Update .env to set environment-specific values.",
  "env_variables": {
    "SECRET_KEY": "admin_api_key",
    "DEBUG": "0 or 1 (0=production, 1=development)",
    "ALLOWED_HOSTS": "*",
    "DB_USE_POSTGRES": "1 to enable Postgres, otherwise SQLite is used",
    "DB_ENGINE": "Django DB engine (default: django.db.backends.postgresql)",
    "DB_NAME": "Postgres database name",
    "DB_USER": "Postgres username",
    "DB_PASSWORD": "Postgres password",
    "DB_HOST": "Postgres host",
    "DB_PORT": "Postgres port (default 5432)",
    "ADMIN_API_KEY": "your-api-key",
    "TIME_ZONE": "Timezone, e.g. UTC"
  },
  "notes": "This JSON is a manifest for editors/automation and is not read by Django at runtime. Use the .env file for runtime configuration."
}
